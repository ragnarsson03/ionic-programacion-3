/**
 * @file index.ts (Barrel Export)
 * @description Punto de entrada único del módulo 'core/models'.
 * Permite importar así: import { ContactMessage } from '@app/core/models';
 * en vez de: import { ContactMessage } from '../../core/models/app.models';
 */
export * from './app.models';
